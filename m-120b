#!/bin/bash
echo -e "\e[1;32m🕶️ [120B-CLOUD] Mr. M is entering the terminal...\e[0m"
echo -e "\e[1;34m🔍 Checking Sovereign Standards...\e[0m"

if [ -f "docs/api-spec.json" ]; then
    echo "✅ [120B] API Contract Found. Good job, Jenderal."
else
    echo "🚨 [120B] ALERT: API Contract Missing!"
fi

# TAMPILKAN FILE YANG GENDUT
FAT_FILES=$(find . -not -path "./api/*" -not -path "./scripts/*" -not -path "*/node_modules/*" -name "*.js" -size +500c)

if [ -z "$FAT_FILES" ]; then
    echo "✅ [120B] CLEAN CODE: All modules are slim & fast!"
else
    echo "🚨 [120B] COMPLEXITY WARNING! These files are too fat:"
    echo "$FAT_FILES" | sed 's/^/- /'
fi

echo -e "\n\e[1;33m📋 NEXT ORDERS FROM MR. M:\e[0m"
echo "1. Fix the fat files listed above."
echo "2. Deploy v3.5 to clear the 'Kaen Nenek' curse."
echo "3. Remember: Sebungkus & Kopi is the fuel for architecture."
